# 服务器API

1. 接收屏主服务器请求「广告位消耗情况上报」接口上报库存，以供及刻智屏系统进行库存查询。
2. 接收屏主服务器请求「获取广告列表」接口获取待播放广告，每15分钟刷新一次。
3. 接收屏主服务器根据广告ID，请求「获取广告对应设备列表」接口获取待播广告需要投放的设备列表，下发播放请求到屏终端。
4. 接收屏主服务器请求「广告播放记录」接口，上报及刻广告服务器。
5. 若广告转化成功，产生用户点击等事件，则接收屏主服务器调用事件监测接口上报。